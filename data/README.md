# Form submissions (private)

`submissions.json` is written by `api/submit.php`.

- **Not public** — `.htaccess` denies web access.
- Each form submit appends one object and emails **mallacootamsar@gmail.com**.
- Download via Hostinger File Manager or SSH when you need a backup / spreadsheet export.

Do not commit real personal data to a public GitHub repo if the file fills up with live responses. The empty `[]` starter is fine in git.
